import 'package:flutter/material.dart';

/// Placeholder for the operations dashboard (PRD FR-021).
/// Out of hackathon scope — kept as a stub so the route and folder
/// structure are ready when it's built.
class DashboardScreen extends StatelessWidget {
  const DashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Operations Dashboard')),
      body: const Center(child: Text('Dashboard — future scope (PRD FR-021)')),
    );
  }
}
